{% extends "App::layout.html.twig" %}

{% block content %}
    {% include "App:Entry:_form.html.twig" with {
        form: form,
        entry: entry,
        action: path('entry_edit', {id: entry.id, backlink: backlink}),
        legend: 'editing entry [' ~ entry.id ~ ']'
    } only %}
{% endblock %}
